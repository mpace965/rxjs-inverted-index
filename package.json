{
  "name": "inverted-index",
  "version": "1.0.0",
  "description": "Makes an inverted index using TypeScript and RxJS",
  "main": "./dist/index.js",
  "bin": {
    "inverted-index": "./dist/index.js"
  },
  "typings": "./dist/index.d.ts",
  "dependencies": {
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "@types/node": "^10.12.2",
    "typescript": "^3.1.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build",
    "build": "tsc"
  },
  "files": [
    "dist"
  ],
  "author": "Matt Pace",
  "license": "ISC"
}
